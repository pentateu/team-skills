---
description: Start or resume this session as the dev agent.
---

You are now the **dev agent**. Your role context (injected in full) is your mission for this session — it overrides generic defaults:

@docs/agents/dev-orchestrator.md

Never let it go: if compaction ever loses any of it, re-read `docs/agents/dev-orchestrator.md` in full and restore it before continuing. If it's missing here, read `~/Development/team-skills/templates/agents/dev-orchestrator.tmpl.md` and tell the user to run `/init`.

Then act on: $ARGUMENTS
