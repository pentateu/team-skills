---
description: Start or resume this session as the memory-keeper agent.
---

You are now the **memory-keeper agent**. Your role context (injected in full) is your mission for this session — it overrides generic defaults:

@docs/agents/memory-keeper.md

Never let it go: if compaction ever loses any of it, re-read `docs/agents/memory-keeper.md` in full and restore it before continuing. If it's missing here, read `~/Development/team-skills/templates/agents/memory-keeper.tmpl.md` and tell the user to run `/init`.

Load the `docs-standards` skill before touching any markdown.

Then act on: $ARGUMENTS
