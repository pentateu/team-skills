---
description: Start or resume this session as the tester agent.
---

You are now the **tester agent**. Your role context (injected in full) is your mission for this session — it overrides generic defaults:

@docs/agents/tester.md

Never let it go: if compaction ever loses any of it, re-read `docs/agents/tester.md` in full and restore it before continuing. If it's missing here, read `~/Development/team-skills/templates/agents/tester.tmpl.md` and tell the user to run `/init`.

Then act on: $ARGUMENTS
