---
description: Refresh team-skills templates and bundled skills from the source projects, then commit in the skills repo.
agent: build
---

Load the `teamctl` skill and run its **sync** workflow in the canonical skills repo (`~/Development/skills`).
